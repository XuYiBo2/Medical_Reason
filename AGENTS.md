# 项目开发原则

本项目属于科研 / 算法实验 / 原型开发，不是企业级生产系统。

## 适用原则

本文件只约束实现方式和工程复杂度，不得缩减、替换或忽略用户明确要求的功能。

如果精简原则与用户当前明确需求冲突，以用户明确需求为准。

## 优先级

开发时按以下顺序优化：

1. 核心功能和算法正确；
2. 主流程清晰、容易理解；
3. 实现简单、容易修改；
4. 在必要范围内保证鲁棒性；
5. 在不牺牲上述目标的前提下，使 diff 尽可能小。

不要默认追求理论上的最大鲁棒性。

## 复杂度预算

任何新增的：

- 分支；
- fallback；
- retry；
- validation；
- abstraction；
- wrapper；
- dependency；
- configuration；

都必须能够回答：

> 它解决了什么当前真实存在的需求或现实风险？

如果没有明确答案，不要加入。

不要为假设中的未来需求提前设计架构。

## Corner Case

默认只处理：

- 正常路径；
- 明显边界；
- 现实中可能出现的重要异常；
- 会导致明显数据损坏或错误结果的问题。

不要为了极低概率且影响轻微的理论 corner case 增加大量代码。低概率但后果严重的问题不属于上述可忽略范围。

如果某个 corner case 值得提醒但不值得实现，在最终说明中指出即可。

## 防御式编程

除非存在真实需求，否则不要主动加入：

- SHA256 / checksum 等完整性验证；
- 多层 fallback；
- retry；
- 大量 try/except；
- 重复 validation；
- 复杂 recovery；
- 为单一实现创建 Interface / Factory / Manager 等抽象。

允许程序在真正异常的程序状态下清晰失败。

## 修改原则

修改已有代码时优先最小改动。

不要顺便：

- 重构无关模块；
- 重新设计架构；
- 创建未来扩展能力；
- 增加企业级基础设施。

保持核心执行流程直接可见。

## 最终检查

完成任务后检查当前 diff：

> 是否存在无法对应当前真实需求或现实风险的复杂度？

如果存在，删除。

最终目标是：

> 满足当前需求的最小、清晰、正确实现。

## 提高防御等级的场景

如果代码涉及：

- 认证与权限；
- 密码学；
- 支付与金融；
- 不可信公网输入；
- 不可恢复的重要数据；
- destructive operations；
- 生产级高可靠服务；

则可以提高防御等级，安全性和正确性优先于简洁性。

## 开发与远程实验工作流

- 代码在本地工作区开发、审查和提交；GPU smoke、训练与实验由用户在远程服务器执行。
- Codex 不假定能够登录远程服务器，也不把本地缺少 32 GB GPU 视为实验通过。
- 每个需要远程 GPU 的阶段必须提供可复制的环境、Git 和运行命令，并生成可回传、可审查的输出或指标文件。
- 远程实验结果未经用户实际运行并回传前，只能标记为“待远程验证”，不得声称 gate 已通过。
- Raw question banks、模型缓存、adapter 和实验 outputs 不提交 Git；代码、配置、锁文件及必要的小型测试 fixture 应提交。
